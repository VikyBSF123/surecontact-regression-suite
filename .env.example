# ─────────────────────────────────────────────────────────────────────────────
# .env.example — copy to .env and fill in your values
# See .env.e2e for the full E2E-specific template (Mailpit, Stripe, Pusher, etc.)
# ─────────────────────────────────────────────────────────────────────────────

# ── Environment ───────────────────────────────────────────────────────────────
ENV=qa
BASE_URL=https://qaing.surecontact.com

# ── Test Credentials ──────────────────────────────────────────────────────────
TEST_EMAIL=vikrantd+autotest1@bsf.io
TEST_PASSWORD=@NGD*!AAXL$mY8C

# ── API ───────────────────────────────────────────────────────────────────────
API_BASE_URL=https://api-qaing.surecontact.com

# ── Mail Catcher (optional — required for email assertion tests) ───────────────
# MAILPIT_URL=http://localhost:8025

# ── Stripe (optional — required for billing/purchase tests) ───────────────────
# STRIPE_PUBLISHABLE_KEY=pk_test_...
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# ── Pusher / Reverb (optional — required for realtime tests) ──────────────────
# PUSHER_APP_KEY=your-pusher-app-key
# PUSHER_HOST=localhost
# PUSHER_PORT=8080
